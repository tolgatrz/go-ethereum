// BlockValidator validates the given blocks
contract BlockValidator {
    enum errors {
    }

    struct header {
    }

    function BlockValidator() {
    }

    function validateHeader() {
    }
}
